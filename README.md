# Web Application using MERN Stack

***Install mongodb***

To run this app:
### Run `cd backend`
### Run `npm install`
### Run `cd frontend`
### Run `npm install`
### Run `node ../backend/server`
### Run `npm start`
